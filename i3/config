# This file has been auto-generated by i3-config-wizard(1).  It will not be overwritten, so edit it as you like.  Should you change your keyboard layout some time, delete this file and re-run i3-config-wizard(1).  i3 config file (v4) Please see https://i3wm.org/docs/userguide.html for a complete reference!  Startup commands

exec_always --no-startup-id /home/nabeel/.config/polybar/launch.sh
#exec --no-startup-id xrandr --dpi 120
#exec "setxkbmap -model pc104 -layout us,ar -variant ,, -option grp:alt_shift_toggle"

# Set default mod key in i3
set $mod Mod4

# Set i3 font
font pango: Roboto Mono Nerd Font 11

# Remove i3 window borders
#new_window 1pixel

# change border size
default_border pixel 2
default_floating_border pixel 2

# Settings for i3-gaps
#gaps inner 10
#gaps outer 5
#gaps top 0

# Start compositor
exec --no-startup-id picom -b &

#start tray indicators (wifi, bluetooth etc)
#exec --no-startup-id nm-applet
#exec --no-startup-id blueman-applet 

#Autostart apps and services
exec --no-startup-id numlockx &
#exec --no-startup-id nitrogen --restore
#exec --no-startup-id killall dunst &
#exec --no-startup-id dunst &
#exec --no-startup-id clipmenud &
#exec --no-startup-id /usr/bin/start-pulseaudio-x11 &
#exec --no-startup-id libinput-gestures-setup start &
#exec --no-startup-id lxpolkit &
#exec --no-startup-id /usr/lib/polkit-1/polkitd --no-debug
#exec_always --no-startup-id autotiling &
#exec --no-startup-id /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1 &
#exec sh ~/.fehbg


#Application shortcuts/keybindings

# Start a terminal
bindsym $mod+Return exec --no-startup-id st 

# Start brave browser
#bindsym $mod+b exec brave
#bindsym Mod1+Ctrl+b exec /usr/bin/brave --incognito

# Start chromium browser
bindsym $mod+c exec --no-startup-id chromium
bindsym Mod1+Ctrl+c exec /usr/bin/chromium --incognito

# Start librewolf browser
bindsym $mod+w exec librewolf

# Start music player
#bindsym $mod+m exec audacious

# Take a screenshoft via xfce4-screenshooter
bindsym Print exec dmenu-screenshot

# Lock screen
bindsym Mod1+ctrl+l exec slock

# exit i3 (logs you out of your X session)
bindsym Mod1+ctrl+e exec dsession

# open lf file manager
bindsym $mod+n exec st -e lf

# open pcmanfm
bindsym $mod+shift+n exec thunar

#start dmenu
bindsym $mod+d exec dmenu_run -i

#emoji picker
bindsym $mod+e exec dmenuemoji

# Start Clipmenu
bindsym $mod+x exec --no-startup-id clipmenu -i

# kill focused window
bindsym $mod+q kill

# Audio controls
bindsym XF86AudioRaiseVolume exec pamixer --allow-boost -i 10 && sound-notification && polybar-msg hook sb-sound 1
bindsym XF86AudioLowerVolume exec pamixer --allow-boost -d 10 && sound-notification && polybar-msg hook sb-sound 1
bindsym XF86AudioMute exec pamixer -t && sound-notification && polybar-msg hook sb-sound 1

# Media player controls
bindsym XF86AudioPlay exec "playerctl --player=playerctld play-pause" && dunst-music-notification
bindsym XF86AudioNext exec "playerctl --player=playerctld next" && dunst-music-notification
bindsym XF86AudioPrev exec "playerctl --player=playerctld previous" && dunst-music-notification

# Brightness controls
bindsym XF86MonBrightnessUp exec xbacklight -inc 5 && brightness
bindsym XF86MonBrightnessDown exec xbacklight -dec 5 && brightness

#i3 keybindings

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# focus mode toggle
bindsym $mod+space focus mode_toggle

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym Mod1+h split h

# split in vertical orientation
bindsym Mod1+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+s layout stacking
#bindsym $mod+w layout tabbed
#bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+Shift+t focus mode_toggle

# focus the parent container
#bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Always use these programs as floating)
#for_window [class="Gnome-calculator"] floating enable
for_window [class="Gcolor2"] floating enable
for_window [class="Gnomecast"] floating enable
for_window [class="Galculator"] floating enable
for_window [class="Nm-connection-editor"] floating enable

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+Home workspace number $ws1
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym Mod1+1 move container to workspace number $ws1
bindsym Mod1+2 move container to workspace number $ws2
bindsym Mod1+3 move container to workspace number $ws3
bindsym Mod1+4 move container to workspace number $ws4
bindsym Mod1+5 move container to workspace number $ws5
bindsym Mod1+6 move container to workspace number $ws6
bindsym Mod1+7 move container to workspace number $ws7
bindsym Mod1+8 move container to workspace number $ws8
bindsym Mod1+9 move container to workspace number $ws9
bindsym Mod1+0 move container to workspace number $ws10

#cycle workspaces
bindsym $mod+Tab workspace next

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
#bindsym $mod+d exec --no-startup-id i3-dmenu-desktop --dmenu='dmenu -h 40'


# reload the configuration file
bindsym $mod+Shift+c reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

#reload polybar
#bindsym Mod1+ctrl+z exec ~/.config/polybar/launch.sh


# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+p mode "resize"


# class                 border  backgr. text    indicator child_border
client.focused          #31748f #31748f #ffffff #31748f   #31748f
client.focused_inactive #333333 #5f676a #ffffff #484e50   #5f676a
client.unfocused        #333333 #222222 #888888 #292d2e   #222222
client.urgent           #2f343a #900000 #ffffff #900000   #900000
client.placeholder      #000000 #0c0c0c #ffffff #000000   #0c0c0c

client.background       #ffffff

#bar {
#    status_command i3blocks
#    position top
#    font pango:Roboto Mono Nerd Font 11
#    tray_output none
#}
